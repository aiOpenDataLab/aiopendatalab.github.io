name: GitHub Actions Data Generator
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  generate_data:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo content
        uses: actions/checkout@v2 # checkout the repository content
      - run: echo "Checkout report content ${{ job.status }}."
        
      - name: Setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11.4'  # install the python version needed
      - run: echo "Setup python ${{ job.status }}."

      - name: install python packages
        #uses: actions/setup-python@v4
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: generate data
        run: python data_gen.py -c stock -o json


  goodbye:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Goodbye World
        run: echo "Goodbye world!"
        shell: bash
